# PLAN

Trinity 프로젝트의 대형/구조적 작업을 추적하는 문서. 완료된 세부 작업의 히스토리가
아니라, **아직 시작 안 했거나 진행 중인 항목**만 남겨둔다. 완료되면 지운다.

## 미착수

### 1. "정책 | 저축플랜" 2대분류 재편

기획서 기준 대분류는 정책/저축플랜 두 개인데, 지금 코드는 `policy_matcher`,
`savings_planner`, `subscription_report`, `card_spending_report` 4개 Tool이
평행하게 나열된 구조다(`CLAUDE.md` "지금 코드가 기획과 다른 점" 참고). 프론트
탭 구조와 백엔드 라우팅을 기획서의 2대분류 + 하위 기능 구조로 재편하는 작업.
**아직 시작 전.**

### 2. 정책 매칭 필터링 — 남은 항목

2026-09-03에 큰 리팩터와 함께 대부분 해결됨(아래 "해결" 참고). 남은 건:

- **장애인/보훈대상자/신혼부부 판별이 계속 정책명 키워드 매칭** — 사용자 확인:
  "키워드 매칭은 텍스트 매칭으로 하자, 어쩔 수 없다." 온통청년 API에 보훈대상자·
  신혼부부를 담는 구조화 필드가 아예 없고(신혼부부), 장애인(`sbizCd` `0014005`)은
  있지만 실측상 채워진 비율이 너무 낮아서(2,750건 중 5~6건) 키워드 방식을
  대체하기엔 부족하다고 판단, 현행 유지로 확정.
  `md_files/DB.md` 부록에 전체 코드표를 옮겨뒀다.
- **`plcyMajorCd`(전공요건)/`earnCndSeCd`(소득조건구분)/`aplyPrdSeCd`/`bizPrdSeCd`
  미사용** — `plcyMajorCd`는 대응하는 유저 프로필 필드(전공)가 아예 없어서 캐시에
  담아도 쓸 데가 없다(새 프로필 필드 추가부터 필요). `aplyPrdSeCd`/`bizPrdSeCd`는
  이미 `apply_start_ymd`/`apply_end_ymd` 기반 날짜 계산(`status.py`)으로
  같은 정보를 얻고 있어 우선순위 낮음.
- **광주/전남 통합 지역(zipCd 접두사 `12`)은 구/군 단위 매칭이 안 됨** — 2025년
  통합으로 구/군 3자리 코드 체계가 완전히 새로 부여됐는데, 확보한 법정동코드
  공식 자료가 개편 이전 것(광주=29, 전남=46)이라 새 코드와의 매핑을 모른다
  (`district_codes.py` 상단 주석 참고). 이 두 지역은 계속 시/도 단위까지만.

**해결된 것**(2026-09-03):
- 혼인상태 필터(`mrgSttsCd` 코드값 실제 반영), 지역코드 템플릿 오염 판별
  (`pvsnInstGroupCd`로 중앙부처/지자체 구분 — 햇살론유스 같은 진짜 전국 상품이
  잘못 걸러지던 문제), `policy_matcher/tool.py`의 불필요한 금융 카테고리 제한 제거.
- **구/군 단위 지역 매칭**: 행정안전부 법정동코드 전체자료(공개 미러)에서 시/군/구
  표를 확보해 `district_codes.py`로 추가 — "서울 강남구"처럼 입력하면 5자리
  법정동코드로 정밀 매칭한다(`region_matches()`). 강원/전북 특별자치도 개편
  접두사 치환 반영, 광주/전남은 위 "남은 항목" 참고.
- **재학생 전용 정책 오노출**("40대인데 국가근로장학금 뜬다"): `schoolCd` 캐시 추가.
- **직업/중소기업 재직 조건**("중소기업 다니는데 상관없는 정책 뜬다"): `jobCd`
  (재직자/자영업자·창업자/미취업자)/`sbizCd`(중소기업 재직) 캐시 추가, 기존
  `occupation`/`is_sme_employee` 프로필 필드와 연결.
- **필터 조건 판정 구조 리팩터**: `is_eligible()`에 조건이 늘 때마다
  `if X(policy) and Y: return False`를 계속 덧붙이던 방식(사용자 지적: "함수
  하나로 퉁칠 수 있는데 왜 예외 케이스를 하나씩 고치냐")을 `TargetingRule` 표
  하나로 교체 — 새 대상군 조건은 이제 표에 한 줄 추가로 끝난다.
- **"정책 매칭 전체 탐색이 클라이언트 사이드 한정"(구 PLAN #9)**: 이 문제를 갖고
  있던 화면(`/policy`의 BrowseTab) 자체가 이미 다른 리팩터로 삭제되고 "정책 달력
  › 정책 전체 보기"(`ai_search.py`)로 대체됐는데, 그쪽은 원래부터
  `db.query(CachedPolicy).all()` 전체를 대상으로 서버사이드 키워드 검색을 하고
  있어서 — 사실상 이미 해결돼 있었다. `lib/api.ts`의 `browsePolicies()`만 아무
  화면에서도 안 쓰이는 죽은 코드로 남아있다(삭제는 안 함).

### 3. `subscription_report` / `card_spending_report`가 완전히 가짜 데이터

`ctx`(유저/DB)를 아예 안 쓰고 입력값과 무관하게 하드코딩된 샘플 리스트("Netflix
(샘플) 17,000원" 등)를 반환한다. 기획서에서도 보류 항목이었고 실제 은행/카드 API
연동 없이는 손댈 수 없다. **아직 시작 전, 우선순위 낮음.**

### 4. 저축플랜에 목표/이력이 저장되지 않음

`SavingsLinkedBenefit`(연결된 정책 혜택)만 영속화되고, 목표 금액·기간·현재까지
실제로 모은 돈은 매번 폼 입력값으로만 즉석 계산된다. "지난달 대비 저축 페이스"
같은 걸 보여주려면 목표/월별 저축 기록을 저장하는 테이블이 새로 필요하다.

### 5. AI 분석 리포트 결과 미저장

정책별 분석은 매번 온디맨드 LLM 호출로만 나오고 결과를 저장하지 않는다(저축플랜에
반영한 것만 `SavingsLinkedBenefit`로 남는다). 새로고침하면 리포트가 사라진다.

### 6. 비밀번호 재설정 기능 미구현

회원탈퇴는 구현 완료(`DELETE /auth/me` — 로컬 계정은 비밀번호 재확인 필수, 소셜
계정은 생략, `PolicyRecommendation`/`SavingsLinkedBenefit` 등 참조 데이터까지
같이 삭제). 남은 건 비밀번호 재설정(이메일 발송 기반 "비밀번호를 잊으셨나요")
뿐 — 이메일 발송 인프라가 전혀 없어 별도 작업이 필요하다.

### 7. 정책 저장/북마크 기능 없음

"저축플랜에 반영"과는 별개로, 관심 정책을 그냥 찜해두는 기능은 없다.

### 8. 관리자 패널이 조회 전용

회원/정책 목록에 수정·삭제 같은 CRUD가 없다.
